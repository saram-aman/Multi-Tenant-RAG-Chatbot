# Multi-Tenant-RAG-Chatbot
Features: Upload PDFs/docs per "tenant" (simulate users/companies), Pinecone namespaces for isolation, FastAPI backend with Redis caching/rate limiting, Next.js frontend chat UI. Add hybrid search, query rewriting, citations, basic eval (faithfulness scores).
